import { observer } from 'mobx-react'
import React from 'react'

interface Props {}

class BetHistory extends React.Component<Props> {
	public render() {
		return <div>Test</div>
	}
}

export default observer(BetHistory)
